<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Only src/debug and src/profile declared INTERNET, which Flutter adds
         for the dev tooling. The main manifest had none, so a *release* APK
         shipped with no network permission at all: Supabase auth, the feed
         query and every storage upload would fail on the judged build while
         working perfectly in `flutter run`. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="richfield"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- richfield://auth/confirmed: where the email-confirmed edge
                 function sends a phone after the "confirm your email" link.
                 supabase_flutter (app_links) exchanges the PKCE code on it
                 for a session; services/email_confirmation.dart shows the
                 welcome. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="richfield" android:host="auth"/>
            </intent-filter>
            <!-- richfield://member/<uid>: QR Connect. The Portfolio shows this
                 link as a QR code; the phone camera opens it here and
                 services/member_link.dart hands the id to RootShell, which
                 pushes that member's profile. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="richfield" android:host="member"/>
            </intent-filter>
            <!-- Keep Flutter's own deep-link routing off: with it on, GoRouter
                 would receive richfield://auth/confirmed as a route and show
                 "Page not found" while supabase_flutter was still signing
                 the member in. app_links delivers the link regardless. -->
            <meta-data android:name="flutter_deeplinking_enabled" android:value="false"/>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
